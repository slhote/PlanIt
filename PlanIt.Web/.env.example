# Local dev default — PlanIt.Api's dev launch profile / dotnet run --urls default.
# Production value (Azure) is a DevOps/Hosting subplan concern, set at build/deploy time.
VITE_API_BASE_URL=http://localhost:5223
